<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Seleccionar Fecha</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Añadido padding-top y box-sizing para que el nav no tape el form */
        body { 
            display: grid; 
            place-items: center; 
            min-height: 90vh; 
            padding-top: 70px; /* Espacio para el nav fijo */
            box-sizing: border-box;
        } 
        form { width: 320px; }
    </style>
</head>
<body>
    <%- include('partials/_nav') %>

    <form action="/list" method="GET" class="form-container">
        <h2>Ver Horario de Clases</h2>
         <% if (locals.error) { %>
            <div class="error-msg"><%= error %></div>
        <% } %>
        <div>
            <label for="start">Fecha Inicio:</label>
            <input type="date" id="start" name="start" min="<%= today %>" value="<%= today %>" required>
        </div>
        <div>
            <label for="end">Fecha Fin:</label>
            <input type="date" id="end" name="end" min="<%= today %>" required>
        </div>
        <div class="refresh-div">
            <input type="checkbox" id="refresh" name="refresh" value="true">
            <label for="refresh">Forzar actualización de datos</label>
        </div>
        <button type="submit" class="btn-primary">Ver Clases</button>
    </form>

    <!-- --- ¡¡¡CORRECCIÓN AÑADIDA!!! --- -->
    <script src="/js/main.js"></script>

</body>
</html>
