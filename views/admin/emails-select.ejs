<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Seleccionar Email</title>
    <link rel="stylesheet" href="/css/style.css">
    <style> body { padding-top: 50px; } .container { max-width: 500px; }</style>
</head>
<body>
    <%- include('../partials/_nav') %>

    <div class="container">
        <form action="/admin/emails/select" method="POST" class="form-container">
            <h2>Seleccionar Email de Reserva</h2>
            <p>Este email se usará para todas las reservas que hagas en esta sesión.</p>

            <div>
                <label for="email">Email Activo</label>
                <select name="email" id="email" style="width: 100%; padding: 10px; font-size: 16px;">
                    <% allEmails.forEach(e => { %>
                        <option value="<%= e.email %>" <%= e.email === currentEmail ? 'selected' : '' %>>
                            <%= e.email %> <%= e.isDefault ? '(Por Defecto)' : '' %>
                        </option>
                    <% }) %>
                </select>
            </div>
            <button type="submit" class="btn-primary">Guardar y Volver</button>
        </form>
    </div>
</body>
</html>
