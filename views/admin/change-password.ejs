<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cambiar Contraseña (Admin)</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body { padding-top: 70px; }
        .container-admin { max-width: 600px; margin: 20px auto; }
    </style>
</head>
<body>
    <%- include('../partials/_nav') %>

    <div class="container-admin">
        <h1>Cambiar Contraseña</h1>
        <p>Estás cambiando la contraseña del usuario: <strong><%= user.username %></strong></p>

        <form action="/admin/users/change-password/<%= user.id %>" method="POST" class="form-container">
            
            <% if (locals.error) { %>
                <div class="error-msg"><%= error %></div>
            <% } %>

            <div>
                <label for="newPassword">Nueva Contraseña:</label>
                <input type="password" id="newPassword" name="newPassword" required>
            </div>
            <div>
                <label for="confirmPassword">Confirmar Nueva Contraseña:</label>
                <input type="password" id="confirmPassword" name="confirmPassword" required>
            </div>
            <button type="submit" class="btn-primary">Establecer Nueva Contraseña</button>
            <a href="/admin/users" class="btn-secondary-link">Cancelar</a>
        </form>

    </div>

    <!-- --- ¡¡¡CORRECCIÓN AÑADIDA!!! --- -->
    <script src="/js/main.js"></script>

</body>
</html>
